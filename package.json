{
  "name": "emplea-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "APK de prueba para la app web Emplea usando Capacitor.",
  "scripts": {
    "sync:web": "node scripts/sync-web.mjs",
    "build": "npm run sync:web",
    "cap:copy": "npm run sync:web && npx cap copy",
    "cap:sync": "npm run sync:web && npx cap sync android",
    "android:add": "npx cap add android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
